# ============================================
# CodeVault 主配置
# 通过 spring.profiles.active 切换环境：dev / prod
# ============================================

spring:
  profiles:
    active: dev

  application:
    name: code-vault

# JWT 配置（各环境共用）
jwt:
  secret: CodeVaultSecretKey2026ForJwtTokenGenerationAndValidation
  expiration: 86400000
